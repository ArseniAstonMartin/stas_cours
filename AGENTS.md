# AGENTS.md

## Cursor Cloud specific instructions

This repository (`stas_cours`) is currently an empty project with only a `README.md`. There are no services, dependencies, build steps, tests, or linting configured yet.

- **No package manager or language runtime** is required at this time.
- **No services** need to be started.
- **No tests or lint checks** exist to run.
- **No environment variables or secrets** are needed.

When source code is added, update this file with instructions for running, testing, and linting the project.
